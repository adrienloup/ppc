import type { AccountState } from '@/src/domains/account/domain/account.type.ts';

export const ACC_KEY = 'account_ppc_emma0_1';

export const ACC_STATE: AccountState = {
  mode: 'system',
  pause: false,
};
