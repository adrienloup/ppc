// import type { MachAction, MachineryState } from '@/src/domains/test/machinery/machinery.type.ts';
//
// export const machineryReducer = (state: MachineryState, action: MachAction): MachineryState => {
//   switch (action.type) {
//     case 'LOAD':
//       return action.payload;
//     case 'BUY_CLIPPER':
//       return { ...state, clipper: state.clipper + 1, clipperCost: action.cost };
//     case 'BUY_MEGA_CLIPPER':
//       return { ...state, megaClipper: state.megaClipper + 1 };
//     default:
//       return state;
//   }
// };
