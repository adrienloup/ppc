// import type { ProdAction, ProdState } from '@/src/domains/production/exp.type.ts';
//
// export const prodReducer = (state: ProdState, action: ProdAction): ProdState => {
//   switch (action.type) {
//     case 'LOAD':
//       return action.payload;
//     case 'AUTO_UPDATE_CLIP':
//       return { ...state, clip: state.clip + action.clip };
//     default:
//       return state;
//   }
// };
