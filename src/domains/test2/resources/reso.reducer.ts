// import type { ResoAction, ResoState } from '@/src/domains/test/resources/reso.type.ts';
//
// export const resoReducer = (state: ResoState, action: ResoAction): ResoState => {
//   switch (action.type) {
//     case 'LOAD':
//       return action.payload;
//     case 'DECREASE_WIRE_STOCK':
//       return { ...state, wire: Math.max(0, state.wire - action.wire) };
//     default:
//       return state;
//   }
// };
